services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - SHARED_PASSWORD=secret
      # Mock Pi for local testing? Or real credentials
      # - PI_HOST=...

